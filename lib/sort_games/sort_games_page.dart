import 'package:flutter/material.dart';

class SortGamesPage extends StatelessWidget {
  const SortGamesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sorteio'),
    );
  }
}
